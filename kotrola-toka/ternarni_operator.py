godine = 15
poruka = "" # dozvoljen pristup / pristup nije dozvoljen

if godine >= 13:
    poruka = "Dozvoljen pristup"
else:
    poruka = "Pristup nije dozvoljen"

poruka = "Dozvoljen pristup" if godine >= 13 else "Pristup nije dozvoljen"

# broj je pozitivan ili broj je negativan
broj = 10
poruka = ""
if broj > 0:
    poruka = "broj je pozitivan"
else:
    poruka =  "broj je negativan"
poruka = "broj je pozitivan" if broj > 0 else "broj je negativan"